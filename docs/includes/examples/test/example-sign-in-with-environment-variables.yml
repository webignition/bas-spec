# examples/test/example-sign-in-with-environment-variables.yml
"open https://www.example.com":
  actions:
    - open "https://www.example.com" in "chrome"

  assertions:
    - browser.url is "https://www.example.com"
    - browser.title is "Example Domain"

"sign in as user":
  actions:
    - set ".sign-in-form .user" to "{{ env.TEST_USER_EMAIL }}"
    - set ".sign-in-form .password" to "{{ env.TEST_USER_PASSWORD }}"
    - click ".sign-in-form .submit"

  assertions:
    - browser.url is "https://www.example.com/account/"
    - browser.title is "Welcome {{ env.TEST_USER_EMAIL }}"
