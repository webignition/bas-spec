# examples/step/google-query-page-model.yml
imports:
  pages:
    google_com: ../page-model/google.com.yml

actions:
  - set "{{ search_input }}" to "{{ query_term }}"
  - click "{{ search_button }}"

assertions:
  - browser.title is "{{ expected_title }}"
