# examples/step/google-query-parameterised.yml
actions:
  - set "{{ elements.search_input }}" to "{{ data.query_term }}"
  - click "{{ elements.search_button }}"

assertions:
  - browser.title is "{{ data.expected_title }}"
