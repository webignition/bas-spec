==================
Browser Properties
==================

Properties for the browser can be accessed through the :ref:`built-in browser object <parameters-built-in>` and can be
referenced in :doc:`actions </actions>` and :doc:`assertions </assertions>`.

----
size
----

The size of the browser window.

-------
Example
-------

.. literalinclude:: includes/examples/step/browser-size.yml
